# Changelog

日期使用 ISO `YYYY-MM-DD`。应用内展示通过 `Intl.DateTimeFormat` 按当前语言格式化，**不要**在 `messages` 里写死「2026年7月21日」这类文案。

维护步骤：

1. 在 `content/changelog.ts` 的 `CHANGELOG_DATES` **最前面**插入新日期  
2. 在 `messages/zh-CN.json`、`messages/en-US.json` 与 `messages/ja-JP.json` 的 `changelog.<date>` 下补充 `title` / `items`  
3. 同步更新本文件（可选，方便 GitHub 阅读）

---

## 2026-08-15 — 代码编辑器与标签页标题

- 新增「代码编辑器」：多窗口编辑 HTML / CSS / JS / TS / JSON；Prism 高亮、查找替换、Emmet（Tab 展开）、保存时格式化、HTML 预览；文件走 VFS，刷新后恢复会话
- 资源管理器与桌面双击代码文件打开编辑器；可保存到 `/Desktop`
- 浏览器标签页标题跟随当前聚焦窗口（无可见窗口或锁屏时回到 Avery Mini OS）

## 2026-08-12 — 数独与游戏目录整理

- 新增「数独」：多难度关卡、笔记/提示/失误限制，记录最佳通关时间，收录于「游戏」
- 数独设置：高亮、隐藏已用数字、选数在先、自动撤销错误、自动清除笔记等，本地持久化
- 数独逐步推导（上/下一步与技法依据）与一键显示答案；可用 `ENABLE_CRACK_DEMO` 隐藏破解入口
- 小游戏源码统一迁入 `features/games/`，与游戏合集入口同目录管理

## 2026-08-05 — 消消乐

- 新增「消消乐」：经典三消交换、多关卡（TS 常量）、4/5 连生成行列/炸弹道具，收录于「游戏」
- 扩展至 20 关循序渐进；关卡可配置普通砖 / 加强砖障碍（周围消除冲击，加强砖两次并裂纹）

## 2026-08-04 — 游戏合集、拼图与多语言

- 新增「格子消消」；修复卡槽与棋盘缩放反馈导致的底部栏抖动
- 内置「游戏」文件夹：扫雷 / 合成大西瓜 / 格子消消 / 图片拼图 / 不规则拼图
- 新增「图片拼图」（滑动 3×3～5×5）与「不规则拼图」（Canvas 邻块磁吸成组、可拖大块）
- 开始菜单「程序」子菜单最大高度 400px
- 新增日 / 繁中 / 俄 / 德 / 法；任务栏语言切换共七种语言
- 应用显示名统一走 `messages.apps`，去掉 register 内重复 titles
- 新增「推箱子」：静态 JSON 关卡（`/game/sokoban/levels.json`）、撤销/重开/选关，收录于「游戏」

## 2026-07-29 — VFS、资源管理器与回收站

- 新增虚拟文件系统（`lib/vfs`）：用户文件统一经适配器存储；bootstrap 含 Desktop / Documents / Pictures / Wallpapers / Trash 等目录
- 记事本、画图、图片、壁纸改为走 VFS；智聊独立 IDB 多会话，并支持 `.chat` 导入导出
- 新增资源管理器：浏览 VFS 路径；图片可打开、设为壁纸、复制/移动、移入回收站
- 图片查看器对接 VFS：路径启动、本地上传入 `/Pictures`、URL 临时预览、删除走 `trash`
- 回收站合并桌面软删除与 `/Trash`；桌面可展示 `/Desktop` 文件图标并按类型打开
- 清理旧业务 IDB 与 legacy 迁移兼容代码；完善 README

## 2026-07-27 — IndexedDB 与线上部署

- 记事本 / 画图 / 壁纸 / 图片查看器 / 智聊会话改为 IndexedDB，去掉服务端 `.data` 落盘
- 智聊 API Key 改为环境变量 `SILICONFLOW_API_KEY`；代理超时与客户端上下文；外链图经 `/api/proxy-image`
- 图片上传上限 10MB；生产构建去掉 turbopack；Yarn 锁文件为准并忽略 `package-lock.json`
- 修复 RSC CVE；修正 `WinCloseIcon` 的 `size` 类型导致的构建失败

## 2026-07-26 — 图片查看器与任务栏

- 图片查看器：本地多图上传、URL 导入、多选浏览与缩略图
- 任务栏窗口按钮横向滚动与拖拽重排

## 2026-07-25 — 屏幕保护

- 设置可开启屏保并配置空闲时长（含「永不」）
- 多种动态画布样式与本地化

## 2026-07-24 — 多选桌面与行情连接

- 桌面图标框选多选；拖放与右键批量操作增强
- K 线托管 WebSocket 订阅
- 移除音乐相关功能

## 2026-07-23 — 窗口路由、备份与行情

- 窗口动态路由：聚焦窗口同步 `/window/{应用}`，无刷新改 URL；深链刷新可重开，后退/前进切换聚焦
- 设置「数据」分区：导出/导入本机偏好与桌面状态 JSON 备份
- 设置侧栏改为显示 / 外观 / 任务栏 / 数据；移除闲置的「桌面」分区
- K 线图表（USDT 永续）；记事本 / 画图 / K 线统一类型化 http 客户端
- 智聊上线：流式对话、历史分页与虚拟滚动、单条删除；任务栏托盘装饰精简

## 2026-07-21 — Toast、吸附与桌面体验

- 全局 Toast：成功 / 失败 / 警告，右上角滑入滑出，最多同时 10 条
- 记事本增删改查结果改为 Toast 提示
- 桌面右键「排列图标」：可靠左或靠右列优先重排
- 小窗边缘软吸附（屏边与其它窗口）；贴边后再拖一段才可超出屏幕；最大化不参与
- 文件夹新建/重命名与窗口体系整理；去掉打开窗口时压暗壁纸的遮罩
- 更新日志改为 ISO 日期 + Intl 本地化显示

## 2026-07-19 — 主题、结构与记事本

- 修复明暗主题下设置 / 文档 / 状态栏颜色异常
- 主题色改为 CSS 变量；Tailwind `dark:` 跟随 next-themes
- 目录整理：`features/`、`lib/`、`components/desktop/`
- 日志、记事本、画图应用上线

## 2026-07-18 — 设置、时钟与国际化

- 设置分区：显示 / 外观 / 任务栏 / 桌面
- 任务栏时钟与日历；壁纸上传与直链导入
- 中英文文案大幅补齐

## 2026-07-16 — Win95 控件库

- 抽出 Win95 风格 UI：按钮、输入框、下拉、面板、复选框与右键菜单
- 任务栏按钮显示应用图标，并按打开顺序排列

## 2026-07-12 — 桌面初版

- Win95 风格桌面：图标、窗口、任务栏
- 扫雷；浅色/深色与中英文
- 文档应用上线
